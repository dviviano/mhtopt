% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mht_test.R
\name{mht_test}
\alias{mht_test}
\title{Perform Hypothesis Tests with Optimal MHT Adjustment}
\usage{
mht_test(
  p = NULL,
  z = NULL,
  alpha_bar,
  model = c("linear", "cobbdouglas"),
  cf_share = 0.46,
  J_bar = 3,
  nm_ratio = 1,
  beta = 0.13,
  iota = 0.075
)
}
\arguments{
\item{p}{Numeric vector. One-sided p-values. Provide either \code{p} or \code{z}.}

\item{z}{Numeric vector. Z-statistics. If provided, p-values are computed as
\code{1 - pnorm(z)}.}

\item{alpha_bar}{Numeric. Benchmark single-hypothesis test size.}

\item{model}{Character. Cost model: \code{"linear"} (default) or \code{"cobbdouglas"}.}

\item{cf_share}{Numeric. Fixed cost share (Linear model). Default 0.46.}

\item{J_bar}{Numeric. Average number of subgroups (Linear model). Default 3.}

\item{nm_ratio}{Numeric. Sample size ratio. Default 1.0.}

\item{beta}{Numeric. Arms elasticity (Cobb-Douglas). Default 0.13.}

\item{iota}{Numeric. Sample size elasticity (Cobb-Douglas). Default 0.075.}
}
\value{
A data frame of class \code{"mht_test"} with columns:
\describe{
\item{p_value}{Input p-values}
\item{reject_optimal}{Rejection under the optimal model-based procedure}
\item{reject_bonferroni}{Rejection under Bonferroni correction}
\item{reject_holm}{Rejection under Holm's step-down procedure}
\item{reject_bh}{Rejection under Benjamini-Hochberg (FDR) control}
\item{reject_unadjusted}{Rejection without adjustment}
}
The object also carries attributes \code{alpha_opt}, \code{alpha_bonf},
\code{alpha_bar}, \code{J}, and \code{model}.
}
\description{
Given p-values or test statistics, applies the model-optimal MHT adjustment
from Proposition 4.1 of Viviano, Wuthrich, and Niehaus (2026) and returns
rejection decisions compared across multiple procedures.
}
\examples{
# Test with p-values
pvals <- c(0.003, 0.015, 0.030, 0.048, 0.120, 0.500)
result <- mht_test(p = pvals, alpha_bar = 0.05)
print(result)

# Test with z-statistics
zstats <- qnorm(1 - pvals)
result2 <- mht_test(z = zstats, alpha_bar = 0.05)

# Cobb-Douglas model
result3 <- mht_test(p = pvals, alpha_bar = 0.05, model = "cobbdouglas")

}
\references{
Viviano, D., Wuthrich, K., and Niehaus, P. (2026). "A Model of Multiple
Hypothesis Testing." arXiv:2104.13367v10. \url{https://arxiv.org/abs/2104.13367}
}
